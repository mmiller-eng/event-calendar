# Copy to .env and fill in values. .env is git-ignored.
# Copy to .env and fill in values. .env is git-ignored.

EVENT_CALENDAR_MODEL=anthropic/claude-sonnet-5
ANTHROPIC_API_KEY=
# OPENAI_API_KEY=

# Optional — enables the live web-search fallback (FR-002)
TAVILY_API_KEY=

# Optional overrides (defaults shown)
# EVENT_CALENDAR_TRUSTED_SOURCES=trusted_sources.yaml
# EVENT_CALENDAR_OUTPUT_DIR=calendars
